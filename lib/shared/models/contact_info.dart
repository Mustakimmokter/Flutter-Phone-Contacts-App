class ContactInfo {
  ContactInfo({
    this.isSelected = false,
    this.avatar = 'a',
    this.name = 'mustakim',
    this.number= '01779-504864',
  });

  late bool isSelected;
  final String? avatar, name, number;
}
